<%= form_with url: "/", method: :get do |form| %>
    <%= form.label :user_id, "user id:" %>
    <%= form.text_field :user_id %>
    <%= form.submit %>
<% end %>

<hr>

<% @photos.each do |photo| %>
    <%= link_to image_tag(Flickr.url_t(photo)), Flickr.url_t(photo)%>
<% end %>